tags:
  - name: Form Uji Model NN
del :
  description: Teks yang akan diproses
parameters:
  - name: input_dataNN
    in: formData
    required: true
    type: string
  # form:
  # action: /input_dataNN
  # method: POST
  # enctype: multipart/form-data
  # input:
  #   - type: file
  #     name: input_dataNN
  #   - type: submit
responses:
    '200':
      description: Successful response
    '400':
      description: Bad Request
    '500':
      description: Internal Server Error
swagger_ui:
  theme: "dark"
  layout: "Standalone"